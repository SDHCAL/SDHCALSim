In ROOT, do :

For generating the tree out of the lcio file :
.L writeTree.C
writeTree("test.000.slcio")
.q

To compile and run timeStudy:

gSystem->AddIncludePath(" -I${LCIO}/include/");
gSystem->Load("${LCIO}/lib/liblcio.so"); 
gSystem->Load("${LCIO}/lib/liblcioDict.so");
.L timeStudy.C+

timeStudy t
t.Loop()

ou

TFile *f=TFile::Open("GIF_simu_5000_gammas_10ms_5mm.root");
TTree *t=NULL; f->GetObject("LCIO",t);
timeStudy ts(t);
ts.Loop();
ts.generateTGraph(0)->Draw("APL");

